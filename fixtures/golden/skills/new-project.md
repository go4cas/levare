---
name: new-project
description: "Stand up a new project — create the repo, clone, write the pointer, capture deploy target and house rules, commit."
scripts: [create-repo.sh]
---

# new-project skill

Run by the Orchestrator to promote an idea into a project (§7). In tests this runs
against a scratch git dir, never real GitHub.
