# Idea — `ideas/<name>.md`

A captured pitch with no project yet.

## Fields

| Field | Type | Required | Nullable | Enum values |
|---|---|---|---|---|
| `name` | string | ✅ | — | — |
| `pitch` | string | — | — | — |
| `tags` | string[] | — | — | — |

## Minimal valid skeleton

```markdown
---
name: example-name
---

Replace this line with the real content.
```

**Body:** Rendered as display prose on the idea's board page; only the frontmatter `pitch` is used on promotion.

---

Generated by `scripts/generate-cheatsheets.ts` from the `idea` schema in `src/validate.ts`.
Do not edit by hand — run `bun run docs:generate`.
