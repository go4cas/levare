# Eval — `evals/<name>.md`

A rubric scoring a work-unit type's output.

## Fields

| Field | Type | Required | Nullable | Enum values |
|---|---|---|---|---|
| `name` | string | ✅ | — | — |
| `unit` | string | — | — | — |
| `rubric` | string[] | — | — | — |

## Minimal valid skeleton

```markdown
---
name: example-name
---

This body is not used by levare — see "Body" below.
```

**Body:** Not used — the frontmatter `rubric` is what's read; the body is stored but never rendered or consumed.

---

Generated by `scripts/generate-cheatsheets.ts` from the `eval` schema in `src/validate.ts`.
Do not edit by hand — run `bun run docs:generate`.
