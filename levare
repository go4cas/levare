#!/usr/bin/env bun
// levare — dev entry wrapper (phase 1). `bun build --compile src/cli.ts` produces the shipped
// single binary of the same name in later phases.
import { runCli } from "./src/cli.ts";
runCli(process.argv.slice(2));
